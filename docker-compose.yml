services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-belote}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-belote_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-belote}
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
