# PostgreSQL (Docker Compose)
POSTGRES_USER=belote
POSTGRES_PASSWORD=belote_dev_password
POSTGRES_DB=belote

# Application
BELOTE_DB_URL=postgres://belote:belote_dev_password@localhost:5433/belote?sslmode=disable
BELOTE_JWT_SECRET=change-me-in-production
BELOTE_PORT=8080
